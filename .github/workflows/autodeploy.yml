name: Hexo Auto Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v3
      with:
        ref: main
        
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Config Git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

    - name: Install Dependencies
      run: |
        npm install hexo-cli -g
        npm install

    - name: Deploy
      # 👇 这里是核心改动：用 sed 命令直接修改 _config.yml 里的链接
      run: |
        sed -i "s|https://github.com/|https://${{ secrets.GITHUB_TOKEN }}@github.com/|g" _config.yml
        
        hexo clean
        hexo g
        hexo d